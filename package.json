{
  "name": "prod-client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start-client": "cd client && npm run start-client",
    "build": "cd client && npm install && npm run build",
    "test": "cd client && npm run test",
    "eject": "cd client && npm run eject",
    "start": "cd server && npm start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
